#formy
  = form_for @manufacturer, remote: true do |f|
    - if @manufacturer.errors.any?
      %div#error_explanation
        = t :error_notice
        %ul
          - @manufacturer.errors.full_messages.each do |message|
            %li
              = message
    = f.text_field :label, placeholder: t( 'forms.label' ), autofocus: true
    = f.text_field :homepage, placeholder: t( 'forms.homepage' )
    #commands
      = f.submit t( :save )
      = f.submit t( :cancel )
