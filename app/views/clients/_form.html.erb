<div id="formy" style="width: 370px;">

<%= form_for(@client) do |f| %>
  <% if @client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% @client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="spacer"></div>
  
  <div class="field">
    <%= f.label :label, placeholder: "Label" %><br>
    <%= f.text_field :label, placeholder: "Description" %>
  </div>
  
  <div id="commands"><div class="spacer"></div>
    <%= f.submit "Create"%>
    <%= f.submit "Cancel" %>
  </div>

<% end %>
</div>
